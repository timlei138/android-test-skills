# App 知识卡：<显示名>

<!-- 用法：新建 App 卡时复制本文件为 <包名>.md（如 com.example.app.md）。
     测试时框架按当前前台包名自动找到对应知识卡注入给 AI/执行者。
     MD 格式（2026-09 起）：任何文本都合法，没有语法错误这回事；
     重点信息用列表，机器（AI）靠 grep + 阅读理解，不 parse 结构。
     原则：写正确的路径优先，坑写在离对应路径最近的地方。 -->

- **app**: com.example.app（必填，知识卡索引键 = 包名）
- **name**: 示例 App
- **最近验证**: 2026-09-02（机型；每次沿链路跑通后更新此日期，过期路径会被降级信任）

## 前置条件

测试这个 App 前必须满足的条件（不满足时用例应标 BLOCKED）。

- 示例: 首次使用需先 `pm clear com.example.app`

## 导航入口

怎么进入各功能页（使用者口述的经验放这里）。

- 功能页A: 主页 → 点击XX → 点击YY
- 功能页B: 从XX页 → 右上角菜单 → YY

## 标准链路

可复用的完整前置流程。生成用例时先 match 这里的链路，命中直接照走，不要重新探路。
有可复用函数的，写明 `cases/<包名>/_flow.py → 函数名()`。

### 示例链路（到某目标页）

1. 步骤一（入口 rid：`com.example.app:id/xxx`）
2. 步骤二（弹窗 → 点「知道了」）
3. 到达目标页「XX」

目标页控件:

- 关键控件: `resource-id`
- 另一控件: `resource-id`

## 界面结构

关键控件的结构与规律（Canvas 控件尤其要记）。

- 示例: 时间选择器是双滚轮，中间高亮为当前选中

## 高效操作

使用者知道的「捷径/技巧」。

- 示例: 改时间优先点按数字，滑动容易过头

## 跨应用出口

点某处会跳到别的 App/系统界面。

- 示例: 点「附件」会打开系统选择器 → 文件管理器

## 验证要点

操作后怎么确认成功。

- 示例: 保存后看列表是否出现新条目
