# App 知识卡：<显示名>

<!-- 用法：新建 App 卡时复制本文件为 <包名>.md（如 com.example.app.md）。
     测试时 AI 按当前前台包名检索对应知识卡并加载进上下文（框架无自动注入，靠 Agent 主动读取）。
     MD 格式（2026-09 起）：任何文本都合法，没有语法错误这回事；
     重点信息用列表，机器（AI）靠 grep + 阅读理解，不 parse 结构。

     卡是"地图"不是"日志"：
     - 按页面/控件组织，探索后更新对应小节；禁止追加带日期/用例号的新小节
       （小节标题里出现用例号或日期 = 组织错了）
     - 每条经验一行结论 + 一行依据；过程叙事（AB 实验、几次实测）不进卡，指向 trace/截图即可
     - 机型实测值（坐标/热区偏移/行距）行尾打机型戳 [机型/版本]，换机型先小探针校准再用

     沉淀三问（写进卡之前过一遍，全过才留）：
     1) 下次同类任务 Agent 会来搜这个词吗？—— 搜不到的写了白写；命中词补进「检索索引」
     2) 不写这条 Agent 会做错或重探吗？—— 常识/显然推论不进卡
     3) 能不能下沉？—— 可执行判定 → states/场景卡；≥2 用例链路 → _flow.py；
        跨 App 系统行为 → _system.md。卡是兜底层，能下沉就下沉 -->

- **app**: com.example.app（必填，知识卡索引键 = 包名）
- **name**: 示例 App
- **验证版本**: 1.2.3（机型）
- **最近验证**: 2026-09-02（写通过情况，如"168/172 两用例通过"。大版本真机回归后
  整卡重写：删过时条目、合并按会话堆出来的段落、刷新本行——让体积收敛而非只增不减）

## 检索索引

触发词平铺（保证 grep/全文检索命中率；新增小节时同步补词）：

滚轮、时间选择器、冲突、权限、导入、弹窗、长按、拖动…

## 前置条件

测试这个 App 前必须满足的条件（不满足时用例应标 BLOCKED）。

- 示例: 首次使用需先 `pm clear com.example.app`

## 导航入口

怎么进入各功能页（使用者口述的经验放这里）。

- 功能页A: 主页 → 点击XX（rid: `com.example.app:id/xxx`）→ 点击YY
- 功能页B: 从XX页 → 右上角菜单 → YY

## 标准链路

可复用的完整前置流程。生成用例时先 match 这里的链路，命中直接照走，不要重新探路。
已有 `_flow.py` 函数的**只写指针 + 函数没覆盖的差异**，不抄步骤（抄写版和代码版迟早漂移，
Agent 不知道信哪份）：

- 到「XX页」: `cases/<包名>/_flow.py → goto_xx()`。函数外差异：
  轮询窗口必须 ≥60s（原因一句话）……

## <页面/控件小节>（按对象组织，不按用例/日期）

页面名或控件名作标题，写该对象的关键控件与规律。跨 App 通用技法不进卡
（SKILL.md「关键技术」节已有），只写本 App 特有差异：

- 示例: 时间选择器左右双滚轮，左=开始右=结束（本 App 特有布局）
- 示例: 完成按钮热区偏移 (+61,+31) [TB323FU/9.0.0.83]（bounds 中心点无反应，换机型先校准）

## 验证要点

操作后怎么确认成功。

- 示例: 保存后看列表是否出现新条目
